def f(a):
    b = ''
    for i in range(len(a)-1,-1,-1):
        b += a[i]
    a = ''
    for j in b:
        if j.isupper():
            a += j.lower()
        if j.islower():
            a += j.upper()
    return a

print(f('QWERty'))